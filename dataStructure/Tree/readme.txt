节点分布相对均衡的二叉树，搜索节点的时间复杂度为O(logn)


堆的插入和删除（上浮和下沉），时间复杂度为O(logn)
构建堆的时间复杂度O(n)
二叉堆用数组存储，父节点parent， 左子节点2*parent + 1, 右子节点2*parent + 2

优先队列不再遵循FIFO （使用二叉堆）入队和出队（即上浮和下沉）的时间复杂度为O(logn)
1、最大优先队列，无论入队顺序，最大元素优先出队
2、最小优先队列，无论入队顺序，最小元素优先出队
入队步骤：在最后一个子节点的位置插入元素，再上浮到合适的位置
出队步骤：把最后一个节点调到堆顶，再下沉到合适的位置
